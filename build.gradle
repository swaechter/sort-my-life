buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:4.0.3'
    }
}

apply plugin: 'java'
apply plugin: 'com.github.johnrengelman.shadow'

shadowJar {
    manifest {
        attributes 'Main-Class': 'ch.swaechter.sortmylife.Application'
    }
}

assemble.dependsOn(shadowJar)

repositories {
    mavenCentral()
}

dependencies {
}
